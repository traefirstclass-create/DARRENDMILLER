# Resend API key — create one at https://resend.com/api-keys
RESEND_API_KEY=

# Where booking requests get emailed
BOOKING_TO_EMAIL=

# Optional: sender address. Must be on a domain verified in Resend.
# Leave unset to use Resend's shared sandbox sender (onboarding@resend.dev).
BOOKING_FROM_EMAIL=
